<div class="flex items-center justify-between w-full" {{ attributes }}>
    <div class="flex gap-2 items-center justify-start">
        <span class="{{ color }} h-3 w-3 rounded-4xl"></span>
        <i class="fa-regular fa-folder"></i>
        <p>{{ name }}</p>
    </div>
    <p class="text-[#6A7282]">{{ tasksCount }}</p>
</div>