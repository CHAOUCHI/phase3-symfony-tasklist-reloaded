{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.priority)}}
    {{ form_row(form.folder)}}
    <button class="btn">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
